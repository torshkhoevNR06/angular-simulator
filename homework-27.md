Задача №01
- CounterService будет зарегистрирован один раз на уровне root-контейнера инжектора и является единым провайдером для всех инстансов. Любые компоненты, что будут его запрашивать, будут запрашивать только один экземпляр, который лежит в root-контейнере, и они будут иметь одно состояние

Задача №02
- Здесь будут созданы два экземпляра, потому что Angular проходится по инжекторам, начиная с текущего компонента, ищет эту зависимость и находит его прямо в компоненте, который зареган в поле providers. Компонент в шаблоне дублируется 2 раза и зависимостей тоже будет 2

Задача №03
- ChildComponent получить экземпляр от родителя(ParentComponent), Ангуляр посмотрит на ChildComponent, увидит, что там нет зарегистрированной локальной зависимости, и пойдет к родителю, ParentComponent, у которого зарегистрирована зависимость. Он возьмет зависимость от родителя и передаст ее ChildComponent

Задача №04
- В лекций ты писал что app.config и provideIn:root работают в пределах одного root environment injector, что будет создан лишь один экземпляр, также потому что зарестрованная зависимость через useExisting при запрашивании что самого сервиса что через его название в поле provide будет выдаваться один и тот же экземпляр зависимости

Задача №05
- useFactory будет вызван при запуске приложения поскольку он зареган в файле app.config который запускается первым при запуске приложения

Задача №06
- Там окажется массив, флаг multi: true сообщает инжектору что такой то токен может иметь несколько значений и формирует массив вместо объекта, получается что в value окажется: ['A', 'B'] или [ 'A' ]

Задача №07
- Если зависимости нету нигде то данная опция просто выдаст null и даст запуститься приложению не падая с ошибкой

Задача №08
- Этот флаг который говорит Ангуляру искать зависимость строго тут, следовательно дальше каково нибудь компонента Ангуляр не пойдёт и не дойдет за root-контейнера в котором есть эта зарестрованная зависимость и компонент мог бы получить его оттуда но в итоге не получит и приложение упадёт с ошибкой

Задача №09
- Этот флаг говорит Ангуляру чтобы игнорировал поиск в этом компоненте и сразу перешёл искать к родителю и выше

Задача №10
- Angular дойдёт до инжектора Null и приложение упадёт с ошибкой потому что данная зависимость нигде не зарегана

Задача №11
- LoggerService получить скорее всего 1 экземпляр, поскольку данная зависимость зарестрованная в root'e и является единым состояние для всех, то получается что и компонент получить один экземпляр которые лежат просто в переменных

Задача №12
1. Существуют 2:
  - Один лежит в root-контейнере
  - Второй зарен локально в компоненте
2. HeaderComponent получить экземпляр который зарен локально у него
3. DashboardComponent думаю что получить именно рутовый поскольку здесь компоненты разделены на ветки отдельная ветка HeaderComponent и DashboardComponent, которые не пересекаются, получается что Ангуляр у HeaderComponent искать не будут и дойдёт сразу до root'a
4. С UserCardComponent будет тоже самое
5. UserCardComponent => DashboardComponent => AppComponent => RootContainer